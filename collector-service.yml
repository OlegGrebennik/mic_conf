server:
  port: 8080
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: https://eureka-host.cfapps.io/eureka/
  instance:
    hostname: peer2   

